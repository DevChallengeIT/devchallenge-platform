<ul>
  <%- if controller_name != 'sessions' %>
    <li>
      <%= link_to t('auth.log_in'), new_session_path(resource_name), class: UI::LINK %>
    </li>
  <% end %>

  <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
    <li>
      <%= link_to t('auth.register'), new_registration_path(resource_name), class: UI::LINK %>
    </li>
  <% end %>

  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <li>
      <%= link_to t('auth.forgot_password'), new_password_path(resource_name), class: UI::LINK %>
    </li>
  <% end %>

  <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <li>
      <%= link_to t('auth.didnt_receive_confirmation_instructions'), new_confirmation_path(resource_name), class: UI::LINK %>
    </li>
  <% end %>

  <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <li>
      <%= link_to t('auth.didnt_receive_unlock_instructions'), new_unlock_path(resource_name), class: UI::LINK %>
    </li>
  <% end %>

  <%- if devise_mapping.omniauthable? %>
    <%- resource_class.omniauth_providers.each do |provider| %>
      <li>
        <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider) %>
      </li>
    <% end %>
  <% end %>
</ul>
